# Shared Packages

This section describes the integration and usage design for each Shared Package consumed by
SonarMark. Shared Packages are software packages produced within the same program and consumed
as dependencies; their internal design is outside the scope of this document. Each item's
entry covers which advertised features are used and how the package is integrated into the
SonarMark build.
